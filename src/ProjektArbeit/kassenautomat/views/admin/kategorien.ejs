<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/admin_style.css">
    <title>Kategorien verwalten</title>
</head>

<body>
    <h1>Kategorien</h1>

    <div style="margin-bottom: 20px;">
        <a href="/admin" class="btn-edit">zur Übersicht</a>
        <a href="/admin/kategorien/add" class="btn-add">+ Neue Kategorie anlegen</a>
    </div>

    <%- include('../partials/error-box') %>

    <div class="card">
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Beschreibung</th>
                    <th>Aktion</th>
                </tr>
            </thead>
            <tbody>
                <% kategorien.forEach(kategorie => { %>
                    <tr>
                        <td><%= kategorie.name %></td>
                        <td><%= kategorie.description %></td>
                        <td>
                            <a href="/admin/kategorien/edit/<%= kategorie.id %>" class="btn-edit">Bearbeiten</a>
                            <form action="/admin/kategorien/delete/<%= kategorie.id %>" method="POST"
                                style="display:inline;" onsubmit="return confirm('Wirklich löschen?')">
                                <button type="submit" class="btn-del">Löschen</button>
                            </form>
                        </td>
                    </tr>
                    <% }) %>
            </tbody>
        </table>
    </div>
</body>

</html>