<!-- <%= JSON.stringify(locals) %> -->

<nav class="top-nav">
    <!-- Categories sind immer klickbar -->
    <a href="/categories" class="nav-item <%= currentPage === 'categories' ? 'active' : '' %>">
        Kategorien
    </a>

    <!-- Tickets nur klickbar, wenn man in einer Category ist (oder schon Items hat) -->
    <% if ( session && session.CategoryName && session.CategoryId ) { %>
        <a href="/categories/<%= session.CategoryId %>" class="nav-item <%= currentPage === 'tickets' ? 'active' : '' %>">Tickets (<%= session.CategoryName %>)</a>
    <% } else { %>
        <span class="nav-item disabled">Tickets</span>
    <% } %>

    <!-- Warenkorb nur klickbar, wenn er nicht leer ist -->
    <% if (cartCount > 0) { %>
        <a href="/cart" class="nav-item <%= currentPage === 'cart' ? 'active' : '' %>">
            Warenkorb <span class="cart-badge"><%= cartCount %></span>
        </a>
    <% } else { %>
        <span class="nav-item disabled">Warenkorb</span>
    <% } %>
</nav>

