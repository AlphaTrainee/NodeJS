        <div class="action-footer">
            <% if (currentPage && currentPage === "tickets") { %>
            <a href="/categories" class="btn-secondary">← Zurück zur Auswahl</a>
            <% } %>
            <a href="/" class="btn-abort">Vorgang abbrechen</a>
        </div>